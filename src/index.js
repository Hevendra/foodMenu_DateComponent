import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import MenuDashboard from './components/menuDashboard.component';

ReactDOM.render(
  <React.StrictMode>
    <MenuDashboard />
  </React.StrictMode>,
  document.getElementById('root')
);